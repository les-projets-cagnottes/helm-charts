apiVersion: v1
kind: Service
metadata:
  name: {{ include "les-projets-cagnottes.fullname" . }}
  labels:
    {{- include "les-projets-cagnottes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "les-projets-cagnottes.selectorLabels" . | nindent 4 }}
